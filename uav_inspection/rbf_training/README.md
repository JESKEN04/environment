# RBF-PID 自适应参数训练模块

该目录独立于主仿真流程，用于完成 50 次 PX4 仿真日志采集、最小二乘法训练 RBF 网络，并导出在线推理参数。

## 目录说明

- `collect_logs.sh`：批量触发 50 次 SITL 飞行并保存日志（需本机 PX4/QGC 环境）。
- `train_rbf.py`：从日志构建误差样本，训练 RBF 权重。
- `rbf_pid_weights.json`：训练输出文件，可被主控制程序加载。

> 训练输入：位置/高度设定值与实际值误差。  
> 训练输出：四环 PID 参数增量（位置环、速度环、姿态环、角速度环）。
